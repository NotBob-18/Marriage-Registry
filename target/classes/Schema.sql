CREATE TABLE MarriageSurvey (
                         id int primary key,
                         name varchar(255),
                         lastName varchar(255),
                         age int,
                         isMarried boolean
);